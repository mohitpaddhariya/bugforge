#!/usr/bin/env bash
# Shim so the skill works whether or not `bf` is on PATH.
# Resolves to agent/bf at the project root.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
root="$(cd "$here/../../.." && pwd)"
exec python3 "$root/agent/bf" "$@"
